name: stocks
format: csv
import:
  file: tickers.csv
  ref: tickers
variants:
  - ratio: 0.5
    data:
      - name: tier
        type: string
        value: "A"
  - ratio: 0.5
    data:
      - name: tier
        type: string
        value: "B"
