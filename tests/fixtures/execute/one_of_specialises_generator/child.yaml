name: child
format: csv
output_file: child
include: { file: parent.yaml, ref: p, ratio: 1.0 }
data:
  - { name: id, ref: p.id }
  # one_of restricts the inherited `generator: word` domain to this finite set.
  - name: status
    ref: p.status
    one_of: ["active", "closed", "pending"]
