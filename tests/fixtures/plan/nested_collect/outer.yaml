name: outer
format: csv
rows: 3
links:
  - file: pool.yaml
    ref: pool
    cardinality: 2
data:
  - name: label
    type: string
    generator: name
  - name: items
    type: list
    content:
      from: pool
      fields:
        - name: item
          refs:
            - pool.item_name
            - {bind: pool.collected_labels, reducer: collect}
